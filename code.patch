diff --git a/src/css/style.css b/src/css/style.css
index 2237762..7c38ac0 100644
--- a/src/css/style.css
+++ b/src/css/style.css
@@ -332,7 +332,7 @@ section[id] {
 }
 
 .portfolio .container {
-  max-width: min(1400px, 92vw);
+  max-width: min(1400px, 94vw);
 }
 
 .portfolio-grid {
@@ -346,12 +346,12 @@ section[id] {
   gap: var(--spacing-lg);
   grid-template-columns: 1fr;
   margin: 0 auto;
-  width: 100%;
+  width: min(1200px, 100%);
 }
 
 @media (min-width: 1024px) {
   .bento-grid {
-    grid-template-columns: minmax(320px, 420px) minmax(0, 1fr);
+    grid-template-columns: minmax(320px, 1fr) minmax(0, 1.7fr);
     align-items: start;
     max-width: 1200px;
   }
@@ -374,6 +374,11 @@ section[id] {
 .portfolio-card--cta .btn { margin-top: var(--spacing-md); }
 .portfolio-card__cta-title { font-size: 1.75rem; font-weight: 800; margin-bottom: var(--spacing-xs); }
 .portfolio-card__cta-desc { color: rgba(255,255,255,0.8); }
+.portfolio-card--cta .portfolio-card__content {
+  display: flex;
+  flex-direction: column;
+  gap: var(--spacing-sm);
+}
 
 .bento-right {
   display: grid;
